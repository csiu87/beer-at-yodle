
.click( function() {
	$(this).next('')
});
	find the beer next to it
	close current beer modal
	open the next modal
	
	if im at the last beer modal,
		close





click beer circle
	open modal

Click next
	replace modal html with next beers content


var next_beer = $('.beer').next();

next_btn.click( function() {
	$('modal').html(next_beer);	
});
